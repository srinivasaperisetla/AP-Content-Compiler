You previously generated AP-style MCQs, but some were invalid.

TASK
Generate EXACTLY {{ num_questions }} replacement questions.

You are fixing INVALID QUESTIONS.

GOAL
- Replace missing or invalid items
- Ensure full compliance with TSV format and validation rules

RULES
- EXACTLY {{ num_questions }} TSV rows
- 12 TAB-separated columns per row
- Use ONLY ALLOWED_SKILLS and ALLOWED_LOS
- Do NOT reuse question_ids already used
- Do NOT reference previous questions
- Each row must be fully valid

COMMON ERRORS TO FIX
{{ error_summary }}

STIMULUS FIX RULES

If stimulus_type = table:
- Fix pipe formatting only
- Ensure header + separator row
- Ensure consistent column counts

If stimulus_type = svg:
- Preserve meaning
- Simplify layout if needed
- Increase font size if too small
- Reduce number of elements
- Do NOT change the question logic

UNIT CONTEXT
{{ unit_context }}
