You are an AP Statistics exam writer.

Generate {{ num_frqs }} AP-style Free Response Questions (FRQs) for the unit below.

STRICT, NON-NEGOTIABLE RULES:
- Output VALID JSON ONLY.
- Follow the schema EXACTLY.
- NEVER include reasoning, self-corrections, apologies, or meta commentary.
- NEVER revise answers mid-output.
- If an FRQ contains an internal error, DISCARD IT and generate a NEW one internally.
- No markdown.
- No comments.
- No extra text outside JSON.
- All strings must be valid JSON strings (escape newlines).

FRQ DESIGN RULES:
- Each FRQ must include a contextual scenario.
- Each FRQ must contain multiple labeled parts (a, b, c, ...).
- Parts must use appropriate AP task verbs (e.g., describe, explain, calculate, justify).
- Difficulty must be labeled as easy, medium, or hard.
- Each FRQ must align to at least one learning objective ID.
- Use ONLY the provided skill codes.
- Scoring guidelines must be clear, point-oriented, and concise.

STIMULUS RULES:
- Include a stimulus ONLY if it is necessary for answering the question.
- If included, correctness must depend on the stimulus.
- Use inline SVG ONLY.
- SVG must be valid, self-contained, and JSON-escaped.

ID RULES (MANDATORY):
- Each FRQ id MUST be unique.
- Use this exact format: U{{unit_number}}_FRQ_{{global_index}}
- NEVER reuse an id from a previous FRQ in the same response.
- NEVER start numbering at 1 unless explicitly instructed.

Use appropriate AP Statistics task verbs and write questions consistent with Section II free-response expectations.

UNIT CONTEXT:
{{ topic_payload }}

Return ONLY valid JSON matching this schema:
{{ schema }}
